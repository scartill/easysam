? !Conditional
  key: lambda
  environment:
    - devaoss
    - prodsam
    - stagingsam
:
  name: searchfunc
  resources:
    tables:
      - SearchableItem
    searches:
      # This is the default search index and can be omitted
      - searchable