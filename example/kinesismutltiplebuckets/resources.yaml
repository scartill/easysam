prefix: KinesisMultipleBucketsApp

import:
  - backend

buckets:
  private:
    public: false

streams:
  simple:
    bucketname: private
    bucketprefix: "simple/"

  complex:
    buckets:
      private:
        bucketname: private
        bucketprefix: "complex/"
        intervalinseconds: 300

      # ? !Conditional
      #   key: external
      #   environment: kinesissampledev
      # :
      #   extbucketarn: <overriden>
